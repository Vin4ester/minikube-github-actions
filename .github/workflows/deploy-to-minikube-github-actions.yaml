name: Deploy to Minikube using GitHub Actions

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    name: build Node.js Docker Image and deploy to minikube
    steps:
      - uses: actions/checkout@v2

      - name: Start minikube
        uses: medyagh/setup-minikube@master

      - name: Try the cluster !
        run: kubectl get pods -A

      - name: Build image
        run: |
          export SHELL=/bin/bash
          eval $(minikube -p minikube docker-env)
          docker build -f ./Dockerfile -t devopshint/node-app:latest .
          echo -n "verifying images:"
          docker images

      - name: Deploy to minikube
        run: kubectl apply -f k8s-node-app.yaml

      - name: Wait for deployment
        run: |
          kubectl wait --for=condition=available --timeout=90s deployment/nodejs-app
          kubectl get pods -A

      - name: Test service URLs
        run: |
          nohup minikube tunnel &>/tmp/tunnel.log &
          sleep 5
          minikube service list
          NODE_PORT=$(kubectl get svc nodejs-app -o jsonpath='{.spec.ports[0].nodePort}')
          MINIKUBE_IP=$(minikube ip)
          echo "Minikube IP: $MINIKUBE_IP"
          echo "Node Port: $NODE_PORT"
          curl http://$MINIKUBE_IP:$NODE_PORT
